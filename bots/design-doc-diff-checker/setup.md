Before this bot can read PDF design documents, the machine must have Python 3 with the pypdf package installed.

1. Check: `python3 -c "from pypdf import PdfReader"`
2. If the import fails, install it: `python3 -m pip install pypdf`
3. Confirm the check passes.

No other setup is required.
